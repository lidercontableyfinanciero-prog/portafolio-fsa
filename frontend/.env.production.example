# Frontend — producción
# URL pública de la API FastAPI. El frontend hace proxy server-side de /api/*
# hacia aquí (rewrites en next.config.mjs). Necesaria en build y en runtime.
NEXT_PUBLIC_API_URL=https://tu-api.example.com
